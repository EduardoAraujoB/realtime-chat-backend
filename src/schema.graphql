type User {
  id: ID!
  name: String!
  email: String!
  password: String!
}

type Query {
  users: [User]!
  user(id: String!): User
}

type Mutation {
  user(name: String!, email: String!, password: String!): User
}
